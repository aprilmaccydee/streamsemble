import { defineConfig } from 'astro/config';

// Served at https://aprilmaccydee.github.io/streamsemble/
export default defineConfig({
  site: 'https://aprilmaccydee.github.io',
  base: '/streamsemble',
  trailingSlash: 'always',
});
